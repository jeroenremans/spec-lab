# S8.2 — Define dark theme token sets (Midnight + Bold Agency)

**Epic:** EP8 — Theme Switcher  
**Priority:** Must-have

---

**As a** developer shipping the theme-switcher feature,  
**I want** Midnight and Bold Agency defined as complete CSS custom property sets including dark-adapted EasyMDE CodeMirror overrides,  
**so that** dark-mode users see a consistent, readable interface in the editor and viewer.

**Acceptance intent:** Both dark themes defined. All tokens present. CodeMirror override class applied. Tag highlight colours re-specified for dark backgrounds — all WCAG AA.

**Dependencies:** S8.1 (establishes pattern).

**Non-goals:** JS switching; switcher UI; localStorage.
